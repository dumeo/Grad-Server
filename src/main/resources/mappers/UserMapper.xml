<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.grad.dao.UserMapper">
    <insert id="addUser" parameterType="User">
        insert into user(uid, username, password,community_name, house_addr, avatar_url, create_date)
        values(#{uid}, #{username} ,#{password} , #{communityName} , #{houseAddr}, #{avatarUrl} ,#{createDate});
    </insert>

    <select id="getUserById" resultType="User">
        select * from user where uid=#{uid};
    </select>


</mapper>