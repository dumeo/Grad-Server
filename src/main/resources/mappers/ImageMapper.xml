<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.grad.dao.ImageMapper">

    <insert id="addImage" parameterType="PostImage">
        insert into img_table(post_id, img_order, url)
        values(#{postId}, #{imgOrder}, #{url});
    </insert>

    <select id="selectImagesByPostId" resultType="PostImage">
        select * from img_table where post_id=#{postId} order by img_order;
    </select>

</mapper>